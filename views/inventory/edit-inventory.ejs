<% if (errors) { %>
  <ul class="notice">
    <% errors.array().forEach(error=> { %>
      <li>
        <%= error.msg %>
      </li>
      <% }) %>
  </ul>
  <% } %>
    <% if (title) { %>
      <h1>
        <%= title %>
      </h1>
      <% } else { res.redirect('/') } %>

        <%- messages() %>
          <%- editInventory %>
            <% if (locals.inv_id) { %>
              value="<%= locals.inv_id %>"
                <% } %>
                  <script src="/js/inv-update.js"></script>